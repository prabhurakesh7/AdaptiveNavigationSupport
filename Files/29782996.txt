screen view google analytics android 
google analytics android v4 attached code whats happening time dispatch local hit appears dashboard screen activity package whats wrong code global tracker xml file activity code 

code told send hit line manually dispatch local hits 