set specific jdk version active mq 
active mq running server wanted upgrade version check documentation states change home env path problem change home setup systems depend jre version set configure jre jdk version active mq running changing global home 

create script export specific wanna call command blockquote export home usr jdk jdk1 home bin jar activemq blockquote define classpath variable load dependency jar remember execution command exporting specific basically forcing execution app update thought earlier activemq scripts bin named blockquote activemq script executing run activemq standalone mode blockquote blockquote env script environment settings including home activemq script system variables script activemq opts activemq opts memory configured env script located directory directory refers bin directory blockquote set home env specific version home find environment variable line script 