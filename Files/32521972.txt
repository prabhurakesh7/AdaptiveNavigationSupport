java8 set global lambdas foreach block 
code set variable foreach block works foreach iterator order disorder lamdas foreach block cool simple iterator complicated thing work 

make outer mutable trick ready beating functional purist team nicer functional approach href http stackoverflow similar sleimans approach 