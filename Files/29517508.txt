persist jsr types spring data jpa 
spring data jpa date time api jsr work vehicles localdatetimes number entities returned loose correlation number h2 entity h2 pre class lang prettyprint override h2 repository h2 pre class lang prettyprint override h2 relevant dependencies h2 pre class lang xml prettyprint override h2 failing test h2 pre class lang prettyprint override list elements list strong strong elements request greater timespan wrong hr h2 edit h2 href http stackoverflow users oliver gierke oliver gierke quick answer fix issue adding org springframework data jpa convert threeten packagestoscan property work properly reference working database related testing configuration 

root widely jpa providers support jsr types box spring data jpa ship jpa converters translate time zoned jsr types legacy persisted working simply register managed jpa classes provider couple standard jpa setup list based setup add package class property youre spring boot adding class annotation trick bit detail href https spring io blog spring data fowler jsr threeten backport support blog post covering features spring data release train named fowler ships 