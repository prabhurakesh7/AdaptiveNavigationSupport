filtering collection stream inside collection stream returning 
h1 background information h1 classes h3 insurance h3 h3 customer h3 h3 customerregistry h3 helper method reduces single hr h1 problem h1 list insurances match list filters belonging customers match list filters unclear code clarify em method inside class em list perform sort reduction matching insurances returned directly added temporary collection 

flatmap avoid reducing predicates 