hard reset git branch jgit 
branches master level1 level1 latest hard reset master level1 git bash command works fine question achieve jgit figure set source target branch scenario cloned master branch pre class lang prettyprint override hard reset level1 pre class lang prettyprint override level1 dont provide jgit 

default resets head reset branch branch pre class lang prettyprint override command current branch point latest commit em level1 em checkout state work directory 