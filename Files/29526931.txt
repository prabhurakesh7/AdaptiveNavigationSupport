org hibernate exception constraintviolationexception multiple manytomany relations 
running code exception complaining caused relations container object element objects remove container class runs fine missing information strong update strong required jointable case multiple relations type strong update strong ducksteps hints comments find workaround issue problem definition generates join table keys element lists saving container generates insert join table constraintviolation exception fix explicitly define join tables work wondering ul li solutions issue li li join table manytomany relations work specification li ul 

reasons ol li constraint column relation table deferrable call persist relation persisted entity relation circular relates relates relates solved reordering h2 handles run problem working postgres li li id generation case doesnt work cascade rules reason doesnt generated id jpa persists id table li ol blockquote information blockquote debug output generated sql statements ideally responses finding point transaction fails table definitions constraint definitions find reason problem case experienced h2 magic making deferrable postgres inserting data circular relations blockquote em update em required jointable case multiple relations type blockquote spec blockquote entity employee mapped table named employee entity patent mapped table named patent join table named employee patent owner join table foreign key columns foreign key column refers table employee type primary key employee foreign key column named employee denotes primary key column table employee foreign key column refers table patent type primary key patent foreign key column named patents denotes primary key column table patent blockquote table derived entity names column names derived relation target column names field names structure join table columns problem blockquote em update em solutions issue blockquote depends case level inheritance single field store data query specific type works type primary xor secondary otoh join tables work depending case additional join bigger table blockquote join table manytomany relations work specification blockquote removing null constraints 