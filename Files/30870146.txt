transactional annotation working spring boot 
transactional working spring boot strong application strong pre class lang prettyprint override strong candidatedao strong pre class lang prettyprint override strong service class strong pre class lang prettyprint override strong controller class strong pre class lang prettyprint override call create method dao system throw strong exception strong pre class lang prettyprint override strong gradle strong file pre class lang gradle prettyprint override link git repository href https github yurii buhryn interviewer rel nofollow https github yurii buhryn interviewer 

spring boot spring boot auto configure things configure datasource entitymanagerfactory transaction manager wrong transaction manager jpa configured simply remove bean definition messing proper tx integration remove h2 auto config h2 account basically reduce class add configure datasource jpa correctly h2 jpa plain hibernate h2 tip plain hibernate api simply jpa remove bean simply change dao h2 adding spring data jpa h2 benefit add spring data jpa mix remove dao completely leave interface moved service class belongs imho repository modified service transactional business logic service remove bean definition reducing method strongly suggest work framework work framework simplify developer live h2 dependencies h2 final suggest removing dependency dependencies starter aspectj aop starter jackson isnt supported anymore adding dependency doesnt add 