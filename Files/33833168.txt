method return multiple values 
situation return values method figure parameters struct case pair change values create class return object explain returning values string enum case ignore issues string comparison logic point return define struct return struct parameters return values elegantly 

jon skeet suggested edited include country make enum carry text conversion functionality store make enum car object text enum values car object convert text enum replace resulting enum carrying make country 