android custom class getapplicationcontext 
developing app lot network requests data retrieval network request issue call couple times fine prior declare class inside activity class asynctask declared separate class imported activity classes needed assuming custom class extending activity resolve pass context parameter instantiate hr work 

blockquote pass context parameter instantiate blockquote pass current parameter custom class h2 1st h2 create single argument constructor custom classs access h2 2nd h2 passing custom class method directly h2 current solution h2 