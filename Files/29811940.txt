realm path order make migration 
realmpath migration versions ul li version realm li li version realm li li version realm migrations li ul make users upgrade version version directly install version updated latest schema version number dont run migration v2 v3 version bump result migration running set realm schema version 

christian realm current migration api rough edges migration increments version number hoping make lot href https github realm realm pull rel nofollow https github realm realm pull 