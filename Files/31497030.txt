concatenate map key values string streams replacements made 
person commanded loop partial tongue cheek end string command line parameters output code ignoring streamy line moment names keys remain unchanged values double quotes existing double quotes escaped close quotes em em space pre partially streams alters entry wrong repeatedly appending em em current output element efficiently streams doesnt alter map entries builder continued year difficulty question difficulty 

guess difficulty ill answer personally dont streams ugly pretty quick simpler loops join values character easily parallelize wanted values sort order random sort map remember objects 