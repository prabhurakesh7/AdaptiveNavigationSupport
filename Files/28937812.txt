docker image executable host 
services packaged separate jar file plan package docker images bundle runtime image rely fact host run image runtime 

volume mount runtime host container makes container portable andrew fox points common base image containers image shared containers meaning copy copy container 