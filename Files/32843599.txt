enum getter returning wrong 
enum type values enum code code runs server days runs fine output code days output print description correct figure type getvalue returning enum enum 

enum mutable calling make enum href https en wikipedia org wiki immutable object rel nofollow immutable makes good sense make immutable making fields final ditching setters 